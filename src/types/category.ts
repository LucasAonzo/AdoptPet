/**
 * Represents a pet category in the application
 */
export interface Category {
  id: string;
  name: string;
  icon: string;
  color?: string;
  backgroundColor: string;
  iconBackground?: string;
} 